define(function(){
    return {
        console: {
            WHOLE_SCHEDULE_UPDATE_INTERVAL: 1000 * 60 * 2,
            QUERY_UPDATE_INTERVAL: 1000 * 60 * 2,
            map: {
                AVAILABLE_TASKS: 0,
                SELECTED_TASK: 1,
                ROUTER_PROJECTION: 2
            }
        }
    };
});